kind: Gateway
apiVersion: networking.istio.io/v1alpha3
metadata:
  name: {{ .Release.Namespace }}-gw
spec:
  servers:
    - port:
        name: http
        number: 80
        protocol: HTTP
      hosts: 
        - dstrategies-back-online-{{ .Release.Namespace }}.apps.cluster-5tgc2.5tgc2.sandbox2406.opentlc.com
  selector:
    istio: ingressgateway